# 4E1.
# the first, y_i ~ Normal(mu, sigma)

# 4E2.
# 2. mu and sigma

# 4E3.
# page
# p(mu, sigma | y ) = p(y | mu, sigma)*p(mu)*p(sigma) / p(y)

# 4E4.
# second one

# 4E5.
# three, alpha beta sigma

# 4M2.
# alist(
#    y ~ dnorm(mu, sigma)
#    mu ~ dnorm(0, 10)
#    sigma ~ dunif(0, 10))

# 4M4.
# y_i ~ N(mu_i, sigma)
# mu_i = a + b * year_i
# a ~ N(178, 100)
# b ~ N(0, 10)
# sigma ~ unif(0, 50)

# 4M6.
# change to sigma ~ unif(0, 8)
